[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show which tests were collected and run
addopts = -v --tb=short

# Don't require __init__.py in test directories
norecursedirs = .git __pycache__ .venv
